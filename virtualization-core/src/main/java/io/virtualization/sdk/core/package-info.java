/**
 * Provider-neutral virtualization SDK: domain model, provider abstraction, capability model and
 * asynchronous operation handling. Pure Java, no framework dependencies — provider modules
 * (Proxmox, Incus, QEMU) and consumer modules (CLI, Spring Boot starter) depend on this module,
 * never the reverse.
 */
package io.virtualization.sdk.core;
